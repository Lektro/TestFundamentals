package Exc3Books.books.authors;

/**
 * Pretty redundant. A real system will never make an enum of all possible authors out there.
 * That's what the objects are for.
 */
public enum AuthorCollection { William_Shakespeare }
